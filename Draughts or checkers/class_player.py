class player:
    def __init__(self, id, name, regdate):
        #'''(str(id+10e10)[1:12])'''
        self.id = id
        self.name = name
        self.regdate=regdate
#    def count_id(self):
#        counter_list = []
#        id = max(counter_list)+1
#        counter_list.append(id)
